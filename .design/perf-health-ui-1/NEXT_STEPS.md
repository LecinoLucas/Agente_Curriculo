## Próximos passos

- coletar métricas agregadas reais por fluxo crítico em endpoint leve de summary, se a operação justificar;
- adicionar alertas operacionais para `json_fallback` limitado no RAG;
- expor resumo leve de budgets excedidos apenas quando existir fonte confiável;
- manter dashboard separado como opção futura apenas se `/admin/health` deixar de comportar a navegação;
- considerar timeline agregada de regressões de call-count por release.
