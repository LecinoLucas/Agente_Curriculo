## Ordem recomendada

1. `PERF-FIX-PIPELINE-1B` — confirmar e, se necessário, corrigir atualização visual do board após avanço de candidato em cenários reais.
2. `PERF-FIX-CANDIDATE-DRAWER-1` — reduzir reloads agregados do overview/drawer de candidato.
3. `PERF-FIX-PIPELINE-2` — virtualização e renderização incremental do Kanban.
4. `PERF-HEALTH-UI-2` — adicionar métricas agregadas reais na aba `Performance` com fonte backend leve.
5. `PERF-FIX-RAG-2` — tornar `pgvector` obrigatório em produção e expor sinal agregado do storage mode.
6. `PERF-FIX-JOBS-2` — avaliar endpoint agregado leve para listagem de vagas se a operação crescer.

## Fases não iniciadas agora

Nenhuma das fases acima deve ser iniciada neste fechamento. Este documento apenas prioriza a sequência.
